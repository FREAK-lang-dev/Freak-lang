-- Checker errors must gate both emitters and linkers.
-- Keep the user statement beyond the prepended-stdlib line clamp.

task main() -> int {
    pilot name: word = "Maverick"
    name = "Meiya"
    give back 0
}

main()
