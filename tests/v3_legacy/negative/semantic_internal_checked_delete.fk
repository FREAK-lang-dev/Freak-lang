task main() {
    if fs::delete_file_checked("not-a-source-api") {
        say "unreachable"
    }
    if freak_internal_delete_file_checked("also-not-a-source-api") {
        say "unreachable"
    }
}
