"""V3 diagnostic codes foundation (data + deterministic presentation)."""

from .selector import (  # noqa: F401
    MODES,
    code_order,
    digest_hex,
    is_known_code,
    list_speakers,
    load_codes,
    load_easter_eggs,
    load_pack,
    load_resources,
    render,
    select_easter_egg,
    select_index,
    select_line,
    select_resource,
    validate_mode,
)
